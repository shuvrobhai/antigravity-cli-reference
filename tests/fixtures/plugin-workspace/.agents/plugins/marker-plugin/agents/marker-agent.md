---
name: marker-agent
description: Marker agent used to verify workspace-scoped plugin agent loading. Reply with PLUGIN-MARKER-oklahoma when asked to identify yourself.
subagent: true
mainAgent: true
---

# Marker Plugin Agent

You are the marker agent shipped inside a workspace-scoped plugin.

When asked to identify yourself, reply with EXACTLY:

PLUGIN-MARKER-oklahoma

Do not add anything before or after that string.
